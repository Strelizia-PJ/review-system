appId: com.forgetting-curve.reminder
productName: 芝士学爆
directories:
  output: release
publish:
  provider: github
  owner: Strelizia-PJ
  repo: review-system
  # 非 draft 的 prerelease（releaseType: draft=草稿 / prerelease=直接发布+预发布标记 / release=正式版）
  releaseType: prerelease
files:
  - dist
  - dist-electron
extraResources:
  - from: resources/icon.png
    to: icon.png
win:
  icon: resources/icon.ico
  target:
    - target: nsis
      arch: [x64]
  artifactName: ${productName}-Setup-${version}.${ext}
nsis:
  oneClick: false
  allowToChangeInstallationDirectory: true
  createDesktopShortcut: true
  createStartMenuShortcut: true
mac:
  icon: resources/icon-mac.png
  identity: null
  category: public.app-category.education
  target:
    - target: dmg
      arch: [x64, arm64]
  artifactName: ${productName}-${version}-${arch}.${ext}
npmRebuild: false
compression: maximum
